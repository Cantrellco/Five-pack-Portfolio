node_modules/
.next/
out/
build/

# test + audit output
/playwright-report/
/test-results/
/.lighthouseci/
/blob-report/
/playwright/.cache/

# generated at build time (npm run data) — committed artefacts live in
# public/field/ and lib/generated/ so `next dev` works on a fresh clone
*.tsbuildinfo
next-env.d.ts

.env*.local
.DS_Store
.vercel
